{
  "name": "school-news-project",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "npm --prefix \"test project\" install",
    "dev": "npm --prefix \"test project\" run dev",
    "build": "npm --prefix \"test project\" run build && node -e \"const fs=require('fs'); fs.cpSync('test project/dist', 'dist', {recursive:true});\"",
    "preview": "npm --prefix \"test project\" run preview"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.1.1",
    "oxlint": "^1.79.0",
    "vite": "^8.3.0"
  }
}
